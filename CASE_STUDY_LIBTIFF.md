# Прогон на реальном проекте: libtiff 4.0.9

Дата: 2026-08-26
Проект: [libtiff v4.0.9](https://gitlab.com/libtiff/libtiff), каталог `libtiff/`
(без `tools/` и `test/`), 59 файлов, ~39 тыс. строк C
Детектор: baseline `static`, обученный на Devign (test accuracy 0.616,
ROC-AUC 0.672), порог 0.5
Module 0: `--inline-depth 0` и `2`, стратегия `priority`

Цель: проверить Module 0 на настоящем многофайловом коде и сверить
срабатывания с реально существовавшими в этой версии уязвимостями.

---

## 1. Module 0 на реальном коде

| | |
|---|---|
| файлов разобрано | 59 |
| функций найдено | 871 |
| корней отобрано | 844 (тривиальных 15, дублей 12) |
| время | **2.25 с** (~390 функций/с вместе с разбором) |
| юнитов с инлайном | 130 из 858 (15.2 %) |
| точек вызова заинлайнено | 394 |
| усечено бюджетом | 266 (31.0 %) |
| рост размера | 202.5 → 236.0 токенов (×1.17) |

### Разрешение вызовов

| Категория | Число | Доля |
|---|---|---|
| `exact_include` | 1188 | 24.8 % |
| `exact_same_file` | 887 | 18.5 % |
| `macro` | 1003 | 20.9 % |
| `never_inline` (libc) | 727 | 15.1 % |
| `unresolved` | 480 | 10.0 % |
| `ambiguous` | 462 | 9.6 % |
| `external` | 52 | 1.1 % |

Разрешено **2075 из 4799 (43.3 %)**. Остальное отвергнуто осознанно, а не
потеряно: пятая часть — макросы (libtiff использует их обильно), 15 % — libc,
который мы не инлайним намеренно (D-004), 10 % — неоднозначные, где мы
предпочитаем не угадывать.

---

## 2. Дефект Module 0, найденный этим прогоном

Первый замер дал `exact_include = 2` при `exact_global = 1186`, то есть
разрешение через видимость заголовков практически не работало.

Причина: сборщик объявлений видел `{` на верхнем уровне, считал его телом типа
и **пропускал блок целиком**. В `tiffio.h` всё после 271-й строки лежит внутри
`extern "C" { ... }` — стандартной обёртки любого C-заголовка. Итог: **1
прототип вместо 175**.

Отказ был тихим: правило видимости не срабатывало, и вызовы разрешались
откатным правилом «найти имя где угодно в проекте». На libtiff ответ совпадал,
но на проекте с одноимёнными `static`-функциями в разных файлах выбор был бы
неверным.

После исправления (коммит `80c9b35`) **1186 вызовов переехали из
`exact_global` в `exact_include`**, а `exact_global` обнулился полностью.

Это второй случай, когда реальный код вскрывает то, чего не видят ни 311
тестов, ни синтетические примеры. Первым было повреждение `dataset/test.jsonl`.

---

## 3. Эффект инлайнинга на решения детектора

Сравнение поюнитно, глубина 0 против 2.

| | |
|---|---|
| сопоставлено юнитов | 858 |
| с инлайном | 130 |
| без инлайна | 728 |
| **из них изменили скор** | **0** ✓ |

Нулевой дрейф на 728 юнитах без инлайна — проверка корректности: где инлайнить
нечего, результат воспроизводится побитово. Значит наблюдаемые изменения
вызваны подстановкой тел, а не случайностью конвейера.

### Направление эффекта

| | |
|---|---|
| сменили вердикт | 22 из 130 |
| SAFE → NEUTRAL (флаг поднят) | 4 |
| NEUTRAL → SAFE (флаг снят) | **18** |
| медиана сдвига скора | **−0.0155** |
| скор вырос / упал | 49 / 81 |
| диапазон | [−0.316, +0.167] |

**Скор чаще падает, чем растёт.** Это противоположно ожиданию и обсуждается в
разделе 5.

### Где скор вырос сильнее всего

| Функция | 0 → 2 | Δ | Заинлайнено |
|---|---|---|---|
| `tif_strip.c:TIFFNumberOfStrips` | 0.313 → 0.480 | +0.167 | `_TIFFMultiply32` |
| `tif_luv.c:LogLuv32toXYZ` | 0.312 → 0.431 | +0.119 | `LogL16toY` |
| `tif_dir.c:_TIFFsetNString` | 0.383 → 0.488 | +0.105 | `setByteArray` |
| `tif_luv.c:LogLuvDecodeStrip` | 0.455 → 0.560 | +0.105 | `TIFFScanlineSize` |
| `tif_read.c:TIFFReadScanline` | 0.497 → 0.600 | +0.103 | `TIFFCheckRead` |
| `tif_predict.c:swabHorAcc32` | 0.333 → 0.430 | +0.097 | `horAcc32` |

Подставляемые функции неслучайны: `_TIFFMultiply32` — проверка переполнения
при умножении, `TIFFCheckRead` — валидация перед чтением, `setByteArray` —
работа с буфером. Скор растёт там, где в callee находится проверка границ или
операция с памятью, то есть механизм отрабатывает по назначению.

---

## 4. Сверка с реальными CVE

Уязвимости, реально существовавшие в 4.0.9 и затрагивающие каталог `libtiff/`
(CVE в `tools/` — `tiffcp.c`, `pal2rgb.c`, `ppm2tiff.c`, `tiff2bw.c` — вне
области скана).

| CVE | Функция | Тип | Скор | Вердикт | Перцентиль |
|---|---|---|---|---|---|
| [CVE-2018-15209](https://bugzilla.redhat.com/show_bug.cgi?id=CVE-2018-15209) | `tif_dirread.c:ChopUpSingleUncompressedStrip` | heap overflow | **0.731** | NEUTRAL | **99 %** |
| [CVE-2018-10963](https://alas.aws.amazon.com/ALAS-2019-1306.html) | `tif_dirwrite.c:TIFFWriteDirectorySec` | assertion / DoS | 0.554 | NEUTRAL | 80 % |
| [CVE-2018-18661](https://www.cvedetails.com/vulnerability-list/vendor_id-2224/Libtiff.html) | `tif_lzw.c:LZWDecode` | NULL deref | 0.506 | NEUTRAL | 66 % |
| [CVE-2018-8905](https://ubuntu.com/security/CVE-2018-8905) | `tif_lzw.c:LZWDecodeCompat` | heap overflow | 0.504 | NEUTRAL | 65 % |
| [CVE-2018-18557](https://www.cvedetails.com/vulnerability-list/vendor_id-2224/Libtiff.html) | `tif_jbig.c:JBIGDecode` | OOB write | 0.498 | **SAFE** | 64 % |

Все пять функций найдены Module 0 и просканированы.

### Как это читать

Флагнуто **4 из 5**. Но самостоятельного смысла это число не имеет: базовая
доля флагов по проекту — **35.9 %**, то есть случайный классификатор с той же
частотой пометил бы 1.8 ± 1.1 функции из пяти. Наблюдаемые 4 — это **+2.1 σ**.

Сигнал есть, но при n = 5 это слабое свидетельство, а не доказательство.

Более информативен перцентиль. Медиана скора по проекту — 0.450, а
`ChopUpSingleUncompressedStrip` (настоящее переполнение кучи) стоит на **99-м
перцентиле**, то есть в верхнем проценте всех 858 функций. Это уже похоже на
осмысленное ранжирование.

Три из пяти при этом лежат в диапазоне 0.498–0.506, то есть буквально на
пороге. Разница между «нашли» и «пропустили» для `LZWDecode` (0.506) и
`JBIGDecode` (0.498) — восемь тысячных. Ни о какой уверенной детекции речи нет.

### Инлайнинг на эти пять не повлиял

У всех пяти скор при глубине 2 идентичен глубине 0: разрешённых для инлайна
вызовов внутри не оказалось (в них преобладают макросы и вызовы libc, которые
мы не подставляем намеренно). То есть на конкретно этих CVE Module 0 не
проявился ни в плюс, ни в минус.

---

## 5. Ограничения этого исследования

**Детектор обучен на другом распределении.** Devign — одиночные функции; юниты
после инлайна длиннее и структурно иные. Падение скора (раздел 3) объясняется
скорее этим, чем свойствами инлайнинга.

**Нет полной разметки.** Мы знаем пять функций с CVE, но не знаем, какие из
остальных 853 безопасны. Поэтому precision/recall посчитать нельзя — только
сравнить с базовой долей.

**Версия и CVE не выверены построчно.** Отнесение CVE к функции взято из
описаний в базах, а не из патчей. Часть CVE 4.0.9 могла быть исправлена в
самом дереве после релиза.

**Порог 0.5 не калибровался.** Три из пяти CVE-функций лежат в пределах ±0.01
от порога, так что итог «4 из 5» чувствителен к его выбору.

**31 % юнитов усечены** бюджетом окна — у них подставленный контекст обрезан.

---

## 6. Что этот прогон дал

Положительное:

- Module 0 работает на настоящем многофайловом C-проекте: 871 функция за 2.25 с,
  43 % вызовов разрешено, остальное явно классифицировано;
- сквозная связка `Module 0 → детектор → вердикт` отработала на 858 юнитах;
- нулевой дрейф на юнитах без инлайна подтверждает детерминированность;
- рост скора концентрируется на функциях, куда подставлены проверки границ;
- самая серьёзная из пяти CVE (heap overflow) получила 99-й перцентиль.

Отрицательное и требующее работы:

- найден дефект с `extern "C"`, тихо ломавший разрешение по заголовкам;
- суммарный эффект инлайнинга на скор **отрицательный**, что указывает на
  несовпадение распределений, а не на пользу контекста;
- детекция на уровне порога: три CVE-функции в пределах ±0.01 от 0.5.

Гипотезы и план проверки — в [HYPOTHESES.md](HYPOTHESES.md).

---

## 7. Воспроизведение

```bash
git clone --depth 1 --branch v4.0.9 https://gitlab.com/libtiff/libtiff.git
python -m interproc.cli --project libtiff/libtiff --inline-depth 2 --stats stats.json
```

```bash
python scan_cli.py --project libtiff/libtiff --inline-depth 2 --dry-run --json \
  --model-path code/saved_models/baseline/checkpoint-best-acc/model.bin \
  --output libtiff_d2.json
```

Источники по CVE: [Ubuntu Security](https://ubuntu.com/security/CVE-2018-8905),
[Red Hat Bugzilla](https://bugzilla.redhat.com/show_bug.cgi?id=CVE-2018-15209),
[Amazon Linux ALAS-2019-1306](https://alas.aws.amazon.com/ALAS-2019-1306.html),
[CVE Details: Libtiff](https://www.cvedetails.com/vulnerability-list/vendor_id-2224/Libtiff.html),
[Changes in TIFF v4.0.10](http://libtiff.maptools.org/v4.0.10.html).

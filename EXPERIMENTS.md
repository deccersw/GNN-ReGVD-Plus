# Эксперименты GNN-ReGVD+

Дата: 2026-08-26
Датасет: Devign (FFmpeg + QEMU), train 21 854 / valid 2 732 / test 2 732, доля
уязвимых 45.8 / 43.4 / 45.9 %
Код: ветка `fix/lora-gradient-flow-and-module0`, коммит `36bc1f4`

Все прежние цифры проекта получены при LoRA-адаптерах, которые **не обучались**
(см. F-01 в [ARCHITECTURE_REVIEW.md](ARCHITECTURE_REVIEW.md)). Данные ниже —
первое достоверное измерение.

---

## 1. Что сравнивается

| Конфигурация | Обучаемых | Признаки узлов графа | Железо |
|---|---|---|---|
| **baseline** `--encoder_mode static` | 476 674 (0.38 %) | замороженная таблица эмбеддингов; трансформер не исполняется | MacBook Air M4, MPS |
| **LoRA** `--encoder_mode contextual --use_lora` | 771 586 (0.62 %), из них 294 912 LoRA | контекстные скрытые состояния GraphCodeBERT | Colab T4, fp16 |

Общие гиперпараметры: ReGCN, 2 слоя, hidden 128, `block_size` 400, окно 5,
`embed_dim` 512, лосс `0.7·BCE + 0.3·SupCon` (τ=0.07), lr 5e-4, seed 42,
эффективный батч 128 в обоих прогонах.

LoRA: rank 8, alpha 16, в `query` и `value` всех 12 слоёв
(24 модуля × 12 288 параметров).

---

## 2. Результат на тестовой выборке

Сравниваются **сохранённые чекпоинты** (`checkpoint-best-acc`), а не лучшие
значения по эпохам: чекпоинт отбирается по accuracy, поэтому весов с эпохи,
где была лучшая AUC, физически не существует.

| Метрика | baseline | LoRA | Δ | Δ/σ |
|---|---|---|---|---|
| accuracy | 0.6160 | **0.6266** | +0.0106 | 1.1 |
| **ROC-AUC** | 0.6717 | **0.6938** | **+0.0221** | **2.1** |
| F1 | 0.5298 | **0.5317** | +0.0019 | — |
| precision | 0.6055 | **0.6273** | +0.0218 | — |
| recall | **0.4709** | 0.4614 | −0.0095 | — |

σ(accuracy) ≈ 0.0093 при n = 2732; σ(AUC) ≈ 0.0104 (приближение Hanley–McNeil,
1255 положительных / 1477 отрицательных).

### Как это следует формулировать

LoRA даёт последовательное улучшение ранжирования: **ROC-AUC +0.022 (≈2σ)**,
при сопоставимой accuracy. Прирост accuracy +0.011 составляет ≈1.1σ и **на
одном сиде статистически не подтверждается**.

Модель становится точнее, но осторожнее: precision +0.022 ценой recall −0.010.

**Ограничение:** один сид, один прогон. Для утверждения о значимости нужны
повторы на нескольких сидах — это главный незакрытый пункт.

---

## 3. Валидация: сохранённые чекпоинты

| Метрика | baseline (эпоха 13) | LoRA (эпоха 9) |
|---|---|---|
| accuracy | 0.6325 | **0.6519** |
| ROC-AUC | 0.6840 | **0.7182** |
| F1 | 0.5308 | **0.5390** |
| precision | 0.5960 | **0.6347** |
| recall | **0.4785** | 0.4684 |
| loss | **0.6372** | 0.6949 |

Направление то же, что на тесте: выигрыш по accuracy, AUC, F1 и precision,
небольшая потеря recall.

---

## 4. Динамика обучения

### baseline (`static`), 28 эпох, остановлен вручную

| эпоха | train_loss | eval_loss | acc | auc |
|---|---|---|---|---|
| 0 | 1.9467 | 0.6867 | 0.5655 | 0.483 |
| **10** | 1.8746 | **0.6273** | 0.6310 | 0.678 |
| **13** | 1.8423 | 0.6372 | **0.6325** | 0.684 |
| 22 | 1.7601 | 0.7182 | 0.6186 | 0.6911 |
| 27 | 1.7242 | 0.7328 | 0.6263 | 0.681 |

`train_loss` падал монотонно все 28 эпох, `eval_loss` достиг минимума на 10-й.
Переобучение с 10-й эпохи; accuracy не улучшалась с 13-й.

### LoRA (`contextual`), 15 эпох

| эпоха | eval_loss | acc | f1 | auc |
|---|---|---|---|---|
| 2 | 0.6632 | 0.5933 | 0.3367 | 0.6394 |
| 3 | 0.6298 | 0.6182 | 0.4910 | 0.6690 |
| **4** | **0.6166** | 0.6325 | 0.5035 | 0.6871 |
| 7 | 0.7042 | 0.6424 | 0.5585 | 0.7058 |
| **9** | 0.6949 | **0.6519** | 0.5390 | **0.7182** |
| 11 | 0.7340 | 0.6457 | 0.5592 | 0.7107 |
| 14 | 0.8885 | 0.6413 | 0.5739 | 0.7090 |

### Сходимость

LoRA берёт лучший результат baseline **втрое быстрее**:

| Порог | baseline | LoRA |
|---|---|---|
| accuracy 0.6325 | эпоха 13 | **эпоха 4** |
| минимум eval_loss | 0.6273 (эпоха 10) | **0.6166 (эпоха 4)** |
| максимум AUC | 0.6911 (эпоха 22) | **0.7182 (эпоха 9)** |

`eval_loss` LoRA на 4-й эпохе уже ниже, чем baseline достиг за все 28.

---

## 5. Стоимость

| | baseline (static) | LoRA (contextual) |
|---|---|---|
| железо | M4 Air, MPS | Colab T4, fp16 |
| батч | 128 | 16 × 8 накопления |
| пиковая память | ~2 ГБ | ~2.5 ГиБ |
| скорость обучения | — | 38.3 образца/с |
| эпоха | ~40 мин (с троттлингом) | ~10 мин |
| чекпоинт | 478 МБ (full) | **5.2 МБ** (slim) |

Замер батча на T4 (seq 400, fp16, fwd+bwd+step):

| batch | память | образцов/с |
|---|---|---|
| 8 | 1.55 ГиБ | 21.3 |
| **16** | 2.45 ГиБ | **38.3** |
| 24 | 3.35 ГиБ | 38.0 |
| 32 | 4.28 ГиБ | 34.6 |
| 64 | 7.85 ГиБ | 26.8 |

Пропускная способность выходит на плато рано и дальше падает: батч 64 стоит
втрое больше памяти и работает на 30 % медленнее батча 16. OOM не наступает.

Локальный замер энкодера (roberta-base, seq 400, fwd+bwd):
M4 CPU 3.9 обр/с, M4 MPS 9.8 обр/с. В режиме `static` M4 быстрее Colab
(~1 мин против ~2 мин на эпоху), в `contextual` — вчетверо медленнее.

---

## 6. Module 0: межпроцедурный инлайнинг

### Сквозная проверка с настоящим детектором

Проект `test_samples/projects/p01_simple`: буфер `char buf[64]` объявлен в
`src/main.c`, а `strcpy` находится в `src/util.c`. По отдельности не видно
ни одной функции.

Модель — сохранённый baseline (`static`), порог 0.5.

| Юнит | depth = 0 | depth = 2 | |
|---|---|---|---|
| `handle_request` | 0.3528 → SAFE | **0.5074 → NEUTRAL** | 3 инлайна, 57→154 токена |
| `log_line` | 0.3970 → SAFE | 0.3970 → SAFE | без изменений |
| `checksum` | 0.4720 → SAFE | 0.4720 → SAFE | без изменений |
| `copy_data` | 0.2096 → SAFE | 0.2096 → SAFE | без изменений |

Инлайнинг сдвинул оценку ровно на той функции, где есть межфайловый контекст,
и достаточно, чтобы перейти порог. Три остальные не изменились ни на тысячную —
им нечего инлайнить.

**Это один синтетический пример, построенный под демонстрацию механизма.** Он
подтверждает, что связка `Module 0 → детектор` работает технически, и ничего
не говорит об эффективности в общем случае. Отдельно стоит отметить, что
`copy_data`, где `strcpy` стоит открытым текстом, получает 0.21 — это
характеристика детектора на Devign, а не инлайнинга.

### Статистика парсера на реальном коде

400 функций Devign (реальный код QEMU/FFmpeg), разложенные по 20 файлам:

| Метрика | Значение |
|---|---|
| parse rate | **100 %** (393 функции) |
| время | 0.21 с на 400 функций (~1900 функций/с) |
| средний размер функции | ~347 токенов (оценка) |
| юнитов усечено | 94 из 388 (**24 %**) |
| вызовов не разрешено | 88 % |

Высокая доля неразрешённых вызовов ожидаема: функции вырваны из разных
проектов и сложены в один корпус искусственно, определений там нет.

**Следствие для дизайна:** средняя функция Devign занимает ~87 % окна детектора,
а четверть не влезает вовсе — ещё до всякого инлайнинга. Приоритизация
call-site (решение D-003) поэтому не оптимизация, а условие работоспособности.

---

## 7. Что измерено не было

- **Повторы на нескольких сидах.** Главный пробел: вывод про LoRA держится
  на одном прогоне, прирост AUC — 2.1σ.
- **Вклад FAISS-головы.** `beta = 0.6` заявлена в README как подобранная
  grid search, но `find_optimal_beta()` ни разу не вызывалась (вызов
  закомментирован в `inference.py`). Неизвестно, добавляет ли k-NN-голова
  что-либо поверх классификатора.
- **Рабочая точка.** Порог 0.5 взят по умолчанию. При recall 0.46 для сканера
  уязвимостей имеет смысл построить PR-кривую и выбрать точку осознанно,
  особенно с учётом песочницы и триаж-головы ниже по пайплайну.
- **Эффективность Module 0.** Devign не годится принципиально — там одиночные
  функции. Нужен Juliet/SARD с разнесёнными по файлам `bad`/`good`.
- **Полный fine-tuning** (`contextual` без LoRA, 125 млн параметров) как третья
  точка сравнения.
- **Задержка инференса.** README заявляет 28 / 34 мс; не перепроверялось.

---

## 8. Воспроизведение

```bash
# baseline
python run.py --output_dir=./saved_models/baseline \
  --model_type=roberta --tokenizer_name=microsoft/graphcodebert-base \
  --model_name_or_path=microsoft/graphcodebert-base \
  --do_train --do_eval --do_test --evaluate_during_training \
  --train_data_file=../dataset/train.jsonl \
  --eval_data_file=../dataset/valid.jsonl \
  --test_data_file=../dataset/test.jsonl \
  --block_size 400 --train_batch_size 128 --eval_batch_size 128 \
  --gnn ReGCN --format uni --window_size 5 \
  --hidden_size 128 --num_GNN_layers 2 --num_classes 1 \
  --encoder_mode static --use_faiss --embed_dim 512 \
  --contrastive_weight 0.3 --contrastive_loss supcon \
  --learning_rate 5e-4 --epoch 50 --seed 42 \
  --early_stopping_patience 5 --early_stopping_metric eval_loss
```

```bash
# LoRA
python run.py --output_dir=./saved_models/lora_contextual \
  --model_type=roberta --tokenizer_name=microsoft/graphcodebert-base \
  --model_name_or_path=microsoft/graphcodebert-base \
  --do_train --do_eval --do_test --evaluate_during_training \
  --train_data_file=../dataset/train.jsonl \
  --eval_data_file=../dataset/valid.jsonl \
  --test_data_file=../dataset/test.jsonl \
  --block_size 400 --train_batch_size 16 --eval_batch_size 16 \
  --gradient_accumulation_steps 8 --fp16 --num_workers 2 \
  --gnn ReGCN --format uni --window_size 5 \
  --hidden_size 128 --num_GNN_layers 2 --num_classes 1 \
  --use_lora --lora_rank 8 --lora_alpha 16 --encoder_mode contextual \
  --use_faiss --embed_dim 512 \
  --contrastive_weight 0.3 --contrastive_loss supcon \
  --learning_rate 5e-4 --epoch 25 --seed 42 \
  --early_stopping_patience 5 --early_stopping_metric eval_loss
```

Colab: [notebooks/colab_train.ipynb](notebooks/colab_train.ipynb).

---

## 9. Что придётся исправить в README и статье

| Утверждение | Заявлено | Измерено |
|---|---|---|
| accuracy с LoRA | 0.928 | **0.6266** |
| прирост ROC-AUC от LoRA | +0.179 | **+0.022** |
| обучаемых параметров | ~152.8K | **771 586** (из них 294 912 LoRA) |
| VRAM при дообучении | ~7.8 ГБ | ~2.5 ГиБ при батче 16, seq 400 |

Прежние значения относятся к конфигурации, где адаптеры имели математически
нулевой вклад, поэтому описывали что угодно, кроме LoRA. Энергопотребление
(~0.23 кВт·ч) и задержка инференса не перепроверялись и также относятся к
режиму, в котором трансформер не исполнялся.
